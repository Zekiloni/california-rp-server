

export interface vehicleComponent { 
   component: number
   value: number
};

export interface vehiclePoint { 
   position: Vector3Mp
   rotation: Vector3Mp
};

export interface numberPlate { 
   plate: string
   issued: number
   expiring: number
};
