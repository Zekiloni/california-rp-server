import { Factions } from "./s.Faction";



export class FactionManager extends Factions {
   static playerInvitePlayer (player: PlayerMp, targetPlayer: PlayerMp) {
      if (targetPlayer.character.faction != 0){
         
      }
   }
}