/*let afkSecondsCount = 0;
const localPlayer = mp.players.local;

setInterval(function () {
    if (!global.menuOpened && localplayer.getVariable('ADMIN') == false) {
      afkSecondsCount++;
      if (afkSecondsCount >= 900) {
         
      }
   }
}, 1000);*/